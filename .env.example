# Local environment variables for the bot.
# IMPORTANT: This file is a template. Copy it to .env and fill in your actual values.
# NEVER commit .env files with real tokens to version control.

# Your Discord Bot Token from https://discord.com/developers/applications
# SECURITY: Rotate this token immediately if it was ever committed to git
DISCORD_TOKEN=your_discord_bot_token_here

# Your Discord Server (Guild) ID
# Use /serverinfo command to get this, or right-click server -> Copy Server ID
# Setting this enables fast guild-only command sync (recommended for development)
# Leave blank or comment out for global sync (takes up to 1 hour to propagate)
GUILD_ID=your_guild_id_here

# Character Data Service Configuration (optional)
# Only needed if running the character data scraper as a separate service
# CHAR_DATA_HOST=127.0.0.1
# CHAR_DATA_PORT=4568
